#set($subject = "$device.name: resultado do comando recebido")
$device.name resultado do comando recebido: $position.getString("result") em $dateTool.format("dd 'de' MMMM HH:mm", $event.eventTime, $locale, $timezone)